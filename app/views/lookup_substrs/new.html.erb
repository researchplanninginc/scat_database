<!-- Validation failure messages -->
<% if @lookup_substr.errors.any? %>
  <% @lookup_substr.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Lookup Substr</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_lookup_substr" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for substr_code -->
      <div class="form-group">
        <label for="substr_code" class="control-label">
          Substr code
        </label>

        <input type="text" id="substr_code" name="substr_code" placeholder="Substr code" class="form-control" value="<%= @lookup_substr.substr_code %>">
      </div>

      <!-- Label and input for description -->
      <div class="form-group">
        <label for="description" class="control-label">
          Description
        </label>

        <input type="text" id="description" name="description" placeholder="Description" class="form-control" value="<%= @lookup_substr.description %>">
      </div>

      <button class="btn btn-success">
        Create Lookup Substr
      </button>
      or
      <a href="/lookup_substrs">Cancel</a>
    </form>
  </div>
</div>
